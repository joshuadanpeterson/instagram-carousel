// Header.js

import React from "react";
import "../css/Header.css"; // Make sure to create a Header.css file for styling

const Header = () => {
	return (
		<header className="header">
			<h1 className="header-title">InstaDreamscape</h1>
		</header>
	);
};

export default Header;
